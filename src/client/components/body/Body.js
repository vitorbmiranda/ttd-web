const React = require('react');
const Button = require('../common/button/Button');

const Body = () => (
  <div>
    <Button
      label="test"
      type="button"
    />
  </div>
);

module.exports = Body;
